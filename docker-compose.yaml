

services:
  db:
    image: postgres:16
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: ${PASSWORD}
      POSTGRES_DB: ${DATABASE}
    container_name: agrosoft_v2

    volumes:
      - ./postgres:/var/lib/postgresql/data